# database configuration
DB_HOST=data.tail5d1e1.ts.net
DB_NAME=
DB_USER=
DB_PASSWORD=

# select active regatta if there are several
#ACTIVE_REGATTA_ID=

# host configuration
HOST_NAME=info.regatta-hd.de

# host letsencrypt certificate locations
HOST_CERT_FILE=/etc/letsencrypt/live/${HOST_NAME}/cert.pem
HOST_PRIVKEY_FILE=/etc/letsencrypt/live/${HOST_NAME}/privkey.pem

AQUARIUS_HOST=aquarius

RUST_LOG=infoportal=INFO,aquarius=INFO
